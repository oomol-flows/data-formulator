version: 0.1.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  llm: 0.2.6
icon: ./icon.png
name: data-formulator
description: Translate structured data into charts based on user-defined goals.
displayName: Data Formulator
repository: https://github.com/oomol-flows/data-formulator
releaseNotes: "fix: remove demo input params"
